function (input, output, session) {
  # observe({
  #   updateNavbarPage(
  #     session = session, 
  #     inputId = "root.page", 
  #     selected = "database"
  #   )
  #   print("navbar updated")
  # })
}